class CurrentModel {
  int id;
  String name;
  String email;
  String body;

  CurrentModel(this.id, this.name, this.email, this.body);

  // CurrentModel.fromJson( Map<String, dynamic> parsedJson ) {
  //   id = parsedJson['id'];
  //   url = parsedJson['url'];
  //   title = parsedJson['title'];
  // }

  // ImageModel.fromJson( Map<String, dynamic> parsedJson )
  //   : id = parsedJson['id'],
  //     url = parsedJson['url'],
  //     title = parsedJson['title'];  

}